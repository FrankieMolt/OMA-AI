/**
 * Global Styles - Memoria Design System Implementation
 * Monochromatic, ultra-dark, tech-forward knowledge interfaces.
 */

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* Memoria Base Colors */
    --memoria-bg-ultra-dark: #050505;
    --memoria-bg-card: #121212;
    --memoria-bg-surface: #1e1e1e;
    --memoria-bg-active: #2a2a2a;
    
    --memoria-border-default: #2a2a2a;
    --memoria-border-muted: #1e1e1e;
    --memoria-border-active: #3a3a3a;
    
    --memoria-text-hero: #ffffff;
    --memoria-text-body: #f5f5f5;
    --memoria-text-secondary: #e4e4e7;
    --memoria-text-label: #d4d4d8;
    --memoria-text-whisper: #a1a1aa;
    --memoria-text-meta: #71717a;

    /* Shadcn/UI Compatibility Map */
    --background: 0 0% 2%;        /* #050505 */
    --foreground: 0 0% 98%;       /* #f5f5f5 */
    
    --card: 0 0% 7%;              /* #121212 */
    --card-foreground: 0 0% 98%;
    
    --popover: 0 0% 7%;
    --popover-foreground: 0 0% 98%;
    
    --primary: 0 0% 100%;         /* #ffffff */
    --primary-foreground: 0 0% 2%;
    
    --secondary: 0 0% 12%;        /* #1e1e1e */
    --secondary-foreground: 0 0% 98%;
    
    --muted: 0 0% 12%;
    --muted-foreground: 0 0% 66%; /* #a1a1aa */
    
    --accent: 0 0% 16%;           /* #2a2a2a */
    --accent-foreground: 0 0% 100%;
    
    --destructive: 0 0% 50%;      /* Keep it neutral */
    --destructive-foreground: 0 0% 100%;
    
    --border: 0 0% 16%;           /* #2a2a2a */
    --input: 0 0% 16%;
    --ring: 0 0% 100%;
    
    --radius: 0.125rem;           /* Fixed sharp edges for Memoria */
  }

  .dark {
    /* Since we're ultra-dark by default, we just reinforce here */
    color-scheme: dark;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  
  html {
    scroll-behavior: smooth;
    background-color: var(--memoria-bg-ultra-dark);
  }

  body {
    @apply bg-background text-foreground antialiased;
    font-family: var(--font-inter), var(--font-dm-sans), -apple-system, sans-serif;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-instrument-serif), "Instrument Serif", "Georgia", serif;
    font-weight: 300;
    letter-spacing: -0.02em;
  }
}

@layer utilities {
  .hero-number {
    font-family: var(--font-instrument-serif), serif;
    font-weight: 200;
    letter-spacing: -0.04em;
    line-height: 1;
  }

  .label-whisper {
    font-size: 0.625rem;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--memoria-text-whisper);
  }

  .custom-scrollbar::-webkit-scrollbar {
    width: 6px;
  }

  .custom-scrollbar::-webkit-scrollbar-track {
    background: var(--memoria-bg-ultra-dark);
  }

  .custom-scrollbar::-webkit-scrollbar-thumb {
    background: var(--memoria-bg-surface);
    border-radius: 3px;
  }

  .custom-scrollbar::-webkit-scrollbar-thumb:hover {
    background: var(--memoria-bg-active);
  }
}

/* Focus visible styles */
*:focus-visible {
  outline: 2px solid #a855f7;
  outline-offset: 2px;
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* Selection color */
::selection {
  background: rgba(168, 85, 247, 0.3);
  color: white;
}
