# This workflow is intentionally empty - using codeql.yml (custom) instead
# GitHub's default CodeQL is disabled to avoid duplicate runs
name: CodeQL (disabled)

on:
  # Disable this workflow completely
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use CodeQL Security instead."
