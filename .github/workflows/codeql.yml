# This workflow is DISABLED - GitHub's default CodeQL (managed) is disabled.
# Custom CodeQL Security workflow is in codeql.yml (different filename)
# This empty file prevents GitHub from auto-creating a default CodeQL workflow
name: CodeQL (disabled)

on: {}  # No triggers - completely disabled

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use CodeQL Security instead."
