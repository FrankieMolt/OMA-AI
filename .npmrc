# npm configuration for OMA-AI

# Prevent auto-installation of peer dependencies
legacy-peer-deps=false

# Use npm registry
registry=https://registry.npmjs.org/

# Next.js configuration
# Force Next.js to run from current directory
next-dev-allow-no-turbopack=false

# Ensure proper workspace root detection
workspaces=false
